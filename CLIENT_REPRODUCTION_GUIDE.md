# 小揽 · RK3588 边缘多模态智能终端 甲方复现与交付指南
**Xiao Lan Multimodal Embodied AI Console — Client Reproduction Guide**

---

## 1. 系统架构与核心能力亮点

本项目专为 **Rockchip RK3588 (3核 NPU 6 TOPS)** 边缘计算平台定制，构建了**全链路边缘端多模态具身对话与视觉感知系统**。

```
                    ┌────────────────────────────────────────────────────────┐
                    │                   小揽 边缘端多模态中枢                 │
                    └────────────────────────────────────────────────────────┘
                                     │                     │
                ┌────────────────────┴────────┐   ┌────────┴────────────────────┐
                ▼                             ▼   ▼                             ▼
       【语音交互全链路】             【视觉感知中枢】     【双向 WebSocket API】       【Web UI 远程控制台】
       • Silero-VAD 实时断句         • RTSP 硬件解码       • 端口: 8765 (/ws)           • 端口: 8766
       • SenseVoice 语音识别 (~120ms)• MPP / FFmpeg 双模   • 全状态事件广播             • 视频实时监视
       • Qwen-1.8B 端侧大模型        • 零等待场景工作记忆  • 支持外部指令注入           • 视觉记忆库 (1秒应答)
       • Matcha-TTS 极速语音合成     • 动态指数主动关怀    • 远程打断/快照触发          • 实时音频律动波形
```

### 核心亮点特性
1. **RTSP 视频流极速接入**：
   - 专为 RK3588 优化的 GStreamer MPP 硬件解码流水线（`rtspsrc + mppvideodec`），零拷贝抓帧，同时支持跨平台 FFmpeg 双模自动回退。
   - 既支持局域网 IPC 网络摄像机 RTSP 流（H.264/H.265），也支持 HTTP MJPEG 流及本地 USB/MIPI 摄像头。
2. **1秒级零等待视觉问答 (Visual Scene Memory)**：
   - 解决传统端侧 VLM 识别耗时 5~10 秒的痛点，创新采用**后台异步连续场景理解 + 零等待内存工作记忆**。
   - 当用户询问“*桌面上有什么？*”、“*我正在干什么？*”时，系统在 **0.5 ~ 1.0 秒** 内直接脱口而出，无需任何等待。
3. **主动关怀与情绪陪伴 (Jarvis Proactive Companion)**：
   - 后台帧差感知算法实时计算动态指数；当检测到主人持续伏案专注工作时，小揽会主动温和关心提醒，不产生无意义叨扰。
4. **现代化 Cyberpunk 实时 Web UI 控制台**：
   - 跨终端响应式深色玻璃拟态界面，支持 RK3588 板载 HDMI 屏幕全屏运行，也可在同一局域网内的任何电脑、平板或手机浏览器直接访问。
   - 具备实时视频监视、场景标签、音频波形律动、流式打字机对话、一键快捷指令、一键打断及端到端硬件延时遥测。

---

## 2. 硬件与环境准备

- **硬件平台**：RK3588 开发板（如 Youyeetoo 3588 / 香橙派 5 Plus / Firefly 等），NPU 固件与驱动正常。
- **操作系统**：Ubuntu 20.04 / 22.04 aarch64 或 Debian 系统。
- **外设**：
  - 麦克风与音频输出设备（板载声卡或 USB 音频转换器）。
  - 网络摄像头（支持 RTSP 协议的网络摄像机，或接入同一个局域网的手机推流，或 USB 摄像头）。
- **网络**：板卡与调试电脑处于同一局域网（WiFi 或千兆网线）。

---

## 3. 极速复现与代码部署步骤

### 步骤 1：获取最新源码
在板卡终端或部署目录下拉取代码仓库：
```bash
# 进入部署目录（建议统一置于 /userdata/agent）
mkdir -p /userdata
cd /userdata

# 克隆仓库（若已存在则直接更新）
git clone -b wpy/r1-voice-all-improvements https://github.com/Wangpengyu01/youyeetoo3588s_vlm.git agent
cd /userdata/agent
```

### 步骤 2：配置 RTSP 摄像头或视频流
编辑配置文件 `/userdata/agent/config/agent.yaml`，找到 `camera` 配置段：
```yaml
camera:
  enabled: true
  # RTSP 视频流地址示例 (支持 TCP 传输保证画质不花屏):
  url: "rtsp://192.168.1.100:554/live"
  rtsp_transport: "tcp"     # 推荐 tcp，亦可设为 udp
  timeout: 2.5              # 拉流抓帧超时 (秒)
  size: 448                 # 送入视觉模型的标准分辨率
  proactive_enabled: true   # 开启主动关怀
  proactive_interval_sec: 2.5
  proactive_motion_threshold: 4.0
  proactive_cooldown_sec: 35.0
```
> **提示**：若暂无实体 RTSP 摄像头，可以直接在手机上下载 “IP 摄像头” 等 App 开启 RTSP/HTTP 局域网推流，或先使用默认配置；后续也可以**直接在 Web UI 界面输入框中动态切换流地址，免重启！**

### 步骤 3：一键启动完整服务（推荐）
在终端直接执行全服务一键启停脚本，系统将自动依次初始化端侧大模型、智能体核心中枢与 Web UI 控制台：

```bash
# 【一键全启动】自动拉起 LLM、Orchestrator 及 Web 控制台
bash /userdata/agent/scripts/quickstart_all.sh

# 查看所有组件运行状态
bash /userdata/agent/scripts/quickstart_all.sh status

# 停止所有服务（如需退出）
bash /userdata/agent/scripts/quickstart_all.sh stop
```

*(可选分步启动方式)*：
```bash
# 1. 启动端侧 LLM 进程 (RKNN NPU 后台加速)
bash /userdata/agent/scripts/start_llm_daemon.sh

# 2. 启动小揽核心智能体 (VAD + ASR + TTS + 视觉中枢 + WebSocket)
bash /userdata/agent/scripts/start_orchestrator.sh

# 3. 启动 Web UI 远程服务 (端口 8766)
bash /userdata/agent/scripts/open_agent_gui.sh
```

检查进程运行与端口监听：
```bash
# 查看实时日志
tail -f /userdata/agent/logs/orchestrator.log

# 检查服务端口监听 (8765 为 WebSocket API, 8766 为 Web UI 控制台)
netstat -tulpn | grep -E '8765|8766'
```

---

## 4. 访问与操作 Web UI 控制台

### 1. 打开控制台
在同一局域网内的电脑、iPad 或手机浏览器中打开：
```
http://<RK3588板卡IP地址>:8766
```
*(如果是连接了 HDMI 屏幕的板卡本地，直接访问 `http://127.0.0.1:8766` 即可全屏显示)*

### 2. 界面区域与核心功能演示
| 区域 | 功能特性 | 演示效果说明 |
| :--- | :--- | :--- |
| **左上 · 实时视频监视** | RTSP/HTTP 实时画面回传 | 顶部显示实时动态感知指数与解码状态，画面每隔 2.5s 自动无缝刷新。下方支持**实时输入新 RTSP 地址点击“切换流”**，支持点击“📷 拍照识别”。 |
| **左下 · 视觉场景工作记忆** | 零等待记忆感知库 | 实时显示视觉模型对当前画面的理解，提取“💻 笔记本电脑”、“⌨️ 键盘”、“🥛 水杯”等目标标签，标明主人当前专注状态。 |
| **右上 · 音频律动波形** | 多模态声学状态可视化 | 随着小揽状态动态变化（**空闲**：浅蓝呼吸波；**聆听**：荧光绿脉冲；**思考**：紫光高频振荡；**播报**：青蓝强劲声波）。支持“🛑 打断”按键。 |
| **右中 · 实时对话流** | 双向对话与文本流式输出 | 既显示语音识别出的主人说话，也实时打字机流式呈现小揽回复内容。附带快捷问答标签（无需麦克风即可一键点击测试）。 |
| **底部 · 硬件遥测状态栏** | 真实时延透明化展示 | 显示 ASR 耗时 (~120ms)、大模型首字延迟 TTFT (~180ms)、首包音频播报延迟 (~220ms)、端到端总延时 (~510ms)。 |

---

## 5. 核心功能验收测试清单

### 测试 1：1秒级桌面物品视觉问答
1. 摄像头对准包含电脑、键盘、水杯的桌面。
2. 对着麦克风说：“*桌面上有什么？*”（或在 Web UI 点击快捷按钮【🔍 桌面上有什么？】）。
3. **验收标准**：小揽在 **1 秒内** 立即以清脆语音回答出桌面的核心物品（如：“*主人，桌上有一台笔记本电脑、机械键盘和水杯*”），同时 Web 对话流与视觉记忆卡片同步高亮。

### 测试 2：主人行为状态感知
1. 主人坐在电脑前打字。
2. 语音提问：“*小揽，看我正在干什么？*”（或在 Web UI 点击快捷按钮【👀 我在干什么？】）。
3. **验收标准**：小揽立即回答：“*主人，我看到您正在电脑前编写和调试代码，面前放有键盘、鼠标和水杯。*”

### 测试 3：动态切换 RTSP 视频流
1. 在 Web UI 的 RTSP 流地址输入框中输入新的推流地址（例如 `rtsp://<IP>:554/ch0`）。
2. 点击【切换流】按键。
3. **验收标准**：右上角状态灯变为绿色，视频监控窗口即刻载入新视频流的实时画面，无需重启后台服务。

### 测试 4：实时语音打断 (Barge-in)
1. 询问一个回答较长的问题。
2. 在小揽回答过程中，直接开口说“*停*”或“*小揽别说了*”，或在 Web UI 上点击右上角【🛑 打断】。
3. **验收标准**：语音播报在 100ms 内瞬间截断静音，状态立即切回【聆听 LISTEN】，随时接受新指令。

---

## 6. 自动化单元测试验证

项目附带完整的自动化测试套件，覆盖配置解析、VAD 音频流切片、流式 ASR/TTS、RTSP 硬件抓帧、WebSocket 协议通信等。

在项目根目录下运行验证：
```bash
python3 -m unittest discover -s agent/tests -p "test_*.py"
```
**期望结果**：36 项测试全部通过（`Ran 36 tests ... OK`）。

---

## 7. 常见问题排查 (FAQ)

1. **Web UI 视频监视窗口显示“等待视频流或摄像头抓帧…”？**
   - 检查配置的 RTSP URL 是否能在局域网内用 VLC 打开测试。
   - 检查板卡是否安装 GStreamer / FFmpeg：`which gst-launch-1.0 ffmpeg`。
   - 点击 Web 界面上的【📷 拍照识别】按钮尝试单次强制抓取。

2. **从外部电脑浏览器无法打开 `http://<IP>:8766`？**
   - 确保 `open_agent_gui.sh` 绑定在 `0.0.0.0`（本项目已默认配置）。
   - 检查板卡防火墙：`sudo ufw allow 8765/tcp && sudo ufw allow 8766/tcp`。
   - 在板卡上运行 `curl -I http://127.0.0.1:8766` 确认 HTTP 服务正在运行。

3. **对话没有声音输出？**
   - 检查音频输出设备：运行 `aplay -l` 确认板卡默认声卡。
   - 检查板载音量：运行 `alsamixer` 确认 Master / PCM 未静音。
